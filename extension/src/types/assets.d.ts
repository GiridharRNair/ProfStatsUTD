/** Parcel resolves image imports to an emitted asset URL. */
declare module "*.png" {
    const source: string
    export default source
}

declare module "*.svg" {
    const source: string
    export default source
}
