# Runtime dependencies for the FastAPI app deployed as Vercel Python
# serverless functions. Vercel installs from this file at the repo root, and
# its parser does not support `-r` includes, so keep this list literal.
# Development and importer tooling lives in requirements-dev.txt.
fastapi>=0.115,<1
httpx>=0.28,<1
uvicorn[standard]>=0.32,<1
